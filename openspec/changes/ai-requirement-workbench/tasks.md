## 1. 项目骨架搭建

- [ ] 1.1 前端项目初始化：Vite + Vue 3 + TypeScript，安装 Pinia、Vue Flow、Markdown 渲染库（marked + highlight.js）
- [ ] 1.2 后端项目初始化：FastAPI + uvicorn，安装 SQLAlchemy、LangGraph、redis-py、aiomysql
- [ ] 1.3 创建 MySQL 表结构（sessions、messages、session_tree、user_config），编写 Alembic 迁移脚本
- [ ] 1.4 配置 Redis 连接池，定义缓存 key 规范（window_stack、active_windows、streaming、session）
- [ ] 1.5 搭建前端全局布局骨架：侧边栏 (Sidebar) + 工作区 (Workspace) + 底部输入框 (InputBar) 的静态占位
- [ ] 1.6 实现窗口管理器 WindowManager（Pinia store）：管理 windows Map、z-index 堆叠顺序、位置/大小/倾斜角度/透明度状态

## 2. 基础对话能力

- [ ] 2.1 实现 LangGraph 对话管理图（chat_graph）：load_history → call_llm → save_message 循环
- [ ] 2.2 实现 SSE 流式响应 API（POST /api/sessions/{id}/chat），前端 fetch + ReadableStream 消费
- [ ] 2.3 实现前端 Markdown 渲染组件（MessageItem），代码块语法高亮
- [ ] 2.4 实现会话 CRUD API（创建/读取/更新/删除），前端调用
- [ ] 2.5 实现多模型配置与切换：用户设置中保存 active_model，后端 graph 根据配置选择 LLM provider
- [ ] 2.6 实现消息复制和重新生成功能

## 3. 窗口堆叠交互

- [ ] 3.1 实现动态窗口创建：createApp() 挂载独立 Vue 实例到动态 DOM 容器
- [ ] 3.2 实现窗口堆叠动画：新窗口弹出 → 旧窗口倾斜（rotate -3°）、灰度（grayscale 0.6）、缩放（scale 0.95）、透明度（opacity 0.6），400ms cubic-bezier 过渡
- [ ] 3.3 实现窗口切换动画：点击侧边栏/画布节点 → 目标窗口飞回正位，当前窗口倾斜退后
- [ ] 3.4 实现窗口拖拽（mousedown/mousemove/mouseup）和边缘缩放（最小 300×200px）
- [ ] 3.5 实现窗口最小化：DOM 销毁但数据保留，侧边栏标记"已折叠"，点击恢复
- [ ] 3.6 实现窗口伪关闭：点击关闭 → DOM 销毁，数据保留 MySQL，session status 更新为 'closed'
- [ ] 3.7 确认输入框固定底部，不受窗口堆叠影响，消息始终发送到当前前台窗口

## 4. 超文本交互系统

- [ ] 4.1 实现名词自动识别：后端在 AI 回复中标记技术名词，以 `__underline__术语__/underline__` 格式返回，前端解析为 `<span class="dashed-underline">`
- [ ] 4.2 实现名词点击：点击带虚下划线的名词 → 弹出迷你卡片（简短解释 + "展开追问" + "收藏"）
- [ ] 4.3 实现短文本划选高亮：mouseup → window.getSelection() → 创建绝对定位蓝色半透明覆盖层，点击外部取消
- [ ] 4.4 实现短文本点击弹窗：点击高亮区域 → 新窗口弹出，标题和首条消息为选中文本，自动绑定父子关系
- [ ] 4.5 实现长文本引用追问：选中文本 > 50 字符 → 点击高亮 → 以引用块插入当前窗口输入区
- [ ] 4.6 实现迷你卡片→完整窗口两步式展开：点击"展开追问" → 卡片飞入堆叠区顶部，旧窗口倾斜退后

## 5. 知识树结构

- [ ] 5.1 实现父子节点自动绑定：划词创建窗口时后端自动设置 parent_id 和 root_id
- [ ] 5.2 实现闭包表维护：session 创建/删除/父子关系变更时自动更新 session_tree 表
- [ ] 5.3 实现树形侧边栏渲染：递归 TreeNode 组件，缩进展示层级，打开中节点亮色、已关闭节点灰色
- [ ] 5.4 实现节点删除：叶子节点直接移除，中间节点删除时子节点 parent_id 提升至被删除节点的父级
- [ ] 5.5 实现面包屑导航：窗口顶部显示"根 / 父 / 当前"路径，点击祖先节点跳转
- [ ] 5.6 实现已关闭窗口恢复：点击侧边栏灰色节点 → 从 MySQL 读取数据 → 重新创建窗口实例

## 6. 画布视图

- [ ] 6.1 集成 Vue Flow 实现无限画布，所有活跃节点平铺为卡片
- [ ] 6.2 实现画布/窗口模式一键切换（快捷键 Tab），切换动画
- [ ] 6.3 实现画布节点交互：拖拽重排、双击打开（切换到窗口模式）、右键菜单（创建子节点/删除/标记待定）
- [ ] 6.4 实现关联线标注：从一个节点拖拽到另一个节点 → 虚线连接（仅视觉，不存数据库）
- [ ] 6.5 实现画布缩放（滚轮，10%~200%）和平移（拖拽空白区域）
- [ ] 6.6 实现悬空节点视觉提示：叶子节点无明确结论时红色边框闪烁

## 7. SDD 生成

- [ ] 7.1 实现 IEEE 830 默认映射引擎：遍历知识树 → 按层级映射到标准章节（根→引言，功能拆解→具体需求，标签→非功能需求/外部接口，未解决→附录）
- [ ] 7.2 实现 SDD 生成图（sdd_graph）：traverse_tree → classify_nodes → map_to_ieee830 → generate_sections → assemble_sdd
- [ ] 7.3 实现 SDD 预览界面：渲染完整文档，支持章节折叠导航
- [ ] 7.4 实现 SDD 导出为 Markdown 文件下载
- [ ] 7.5 实现自定义映射配置界面：拖拽标签→章节，保存到 user_config.sdd_mapping_rules
- [ ] 7.6 实现编号样式选择（标准/中文）和 EARS 格式开关

## 8. 质检 Agent

- [ ] 8.1 实现完整性检查：遍历叶子节点，标记无明确结论的节点，侧边栏红色角标
- [ ] 8.2 实现一致性检查：检测同一功能存在冲突方案的节点对
- [ ] 8.3 实现可测试性检查：扫描模糊词汇（"快速"、"友好"、"高效"、"各种"），SDD 中黄色高亮
- [ ] 8.4 实现 EARS 合规率统计：计算功能需求中符合 EARS 格式的比例
- [ ] 8.5 实现质检报告交互界面：列出问题，点击"去解决"→ 自动创建子窗口追问，点击"继续生成"→ 跳过
- [ ] 8.6 实现质检触发流程：用户点击"生成 SDD"→ 先执行质检 → 展示报告 → 用户确认后生成 SDD

## 9. AI 建议面板

- [ ] 9.1 实现建议面板 UI：侧边栏下部常驻，折叠/展开，不打断用户操作
- [ ] 9.2 实现遗漏维度检测：分析知识树，发现已探索功能未覆盖的维度（如提到了支付但没提到认证）
- [ ] 9.3 实现悬空节点提醒：统计并展示待确认节点数量
- [ ] 9.4 实现探索停滞检测：5 分钟无新节点/无消息 → 温和建议
- [ ] 9.5 实现采纳建议流程：点击"帮我分析"→ 自动创建子窗口，AI 开始分析该维度

## 10. 数据持久化与配置

- [ ] 10.1 实现会话和消息的 MySQL 读写（CRUD），确保用户操作立即持久化
- [ ] 10.2 实现 Redis 活跃窗口缓存：窗口栈顺序、活跃窗口集合、流式请求锁
- [ ] 10.3 实现 Redis→MySQL 数据重建：Redis 重启后从 MySQL 恢复活跃窗口缓存
- [ ] 10.4 实现用户配置 API：API Key 加密存储、模型选择、EARS 开关、编号样式、映射规则
- [ ] 10.5 实现设置面板 UI：模型选择器、API Key 输入（加密显示）、映射规则配置、EARS 开关

## 11. 验收测试

- [ ] 11.1 用户故事验收：场景 A（探索新概念）→ 划词 "闭包" → 弹出迷你卡片 → 展开完整窗口 → 知识树新增子节点
- [ ] 11.2 用户故事验收：场景 B（深度挖掘）→ 在"闭包"窗口划词"词法环境"→ 新窗口弹出 → 侧边栏显示祖孙层级
- [ ] 11.3 用户故事验收：场景 C（关闭与恢复）→ 关闭"词法环境"窗口 → 侧边栏灰色节点 → 点击恢复完整对话
- [ ] 11.4 性能验收：划词高亮响应 < 300ms，窗口动画 400ms 流畅，首屏加载 < 2s
- [ ] 11.5 兼容性验收：Chrome 90+、Edge 90+、Firefox 88+ 正常运行
- [ ] 11.6 质检验收：完整 SDD 生成流程 — 创建知识树 → 点击"生成 SDD"→ 质检报告 → 修复问题 → 最终 SDD 预览 → 导出